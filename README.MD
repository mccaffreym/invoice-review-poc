# Invoice Review POC
   AI-powered legal invoice line-item reviewer. Built as a learning project
   to apply for AI PM roles at legal tech companies.
   
   ## Setup
   - Python 3.13, virtualenv in `.venv`
   - `pip install -r requirements.txt` (TODO: create this)
   - Copy `.env.example` to `.env` and add ANTHROPIC_API_KEY